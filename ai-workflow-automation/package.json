{
  "name": "ai-workflow-automation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "pnpm --filter ai-workflow-automation-frontend start",
    "build": "pnpm --filter backend build && pnpm --filter frontend build",
    "test": "pnpm --filter backend test && pnpm --filter frontend test"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  },
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ]
}