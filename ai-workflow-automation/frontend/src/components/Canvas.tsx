import React from 'react';

const Canvas: React.FC = () => {
    return (
        <div className="canvas">
            {/* This is where the workflow design interface will be implemented */}
        </div>
    );
};

export default Canvas;